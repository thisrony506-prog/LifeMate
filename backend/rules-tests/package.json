{
  "name": "life-mate-rules-tests",
  "private": true,
  "scripts": {
    "test": "firebase emulators:exec --project demo-life-mate --config ../firebase.json --only firestore,storage \"node --test --test-concurrency=1 $PWD/rules.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "4.0.1",
    "firebase": "11.10.0",
    "firebase-tools": "14.10.1"
  }
}
